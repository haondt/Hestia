@code {
    [Parameter]
    public bool CloseMainModal { get; set; } = true;
}

@if(CloseMainModal)
{
    <div
        id="modal-container"
        class="modal"
        hx-swap-oob="true">
    </div>
}

